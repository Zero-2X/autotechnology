# deploy/environments/staging

Staging configuration templates only; never commit credentials. `database.env.example` and `storage.env.example` declare only non-secret tuning; database/storage endpoints and credentials must be injected at runtime.

This directory is a V2 boundary created by FOUND-001. Add implementation only under the owning task card and preserve the synthetic/account-free constraints.

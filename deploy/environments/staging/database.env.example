# Staging template only. Inject DATABASE_URL through the secret manager at runtime.
# This placeholder intentionally contains no host, username, password, or token.
DATABASE_CONNECT_TIMEOUT_SECONDS=3
DATABASE_APPLICATION_NAME=ai-content-workflow-staging

# deploy/environments/dev

Development configuration templates only; never commit credentials. `database.env.example` contains the non-secret PostgreSQL shape owned by FOUND-003A; `storage.env.example` contains the non-secret S3-compatible shape owned by FOUND-003B.

This directory is a V2 boundary created by FOUND-001. Add implementation only under the owning task card and preserve the synthetic/account-free constraints.

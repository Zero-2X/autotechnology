# infra/compose

Local dependency composition templates; no credentials. `postgresql.dev.yaml` provides the account-free PostgreSQL 16 development shape owned by FOUND-003A; `storage.dev.yaml` provides the S3-compatible local shape while credentials remain runtime-injected.

This directory is a V2 boundary created by FOUND-001. Add implementation only under the owning task card and preserve the synthetic/account-free constraints.

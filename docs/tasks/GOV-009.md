# GOV-009 — 明确真实账号获取这个外部依赖的 Owner、最晚到位时间和无账号替代方案。

状态：`done`  
阶段：0（范围、决策和治理冻结）  
Owner：`team/governance`  
优先级：`critical` / `P0`

## 目标

明确真实账号获取这个外部依赖的 Owner、最晚到位时间和无账号替代方案。

## 明确不做

- 不修改任务注册表中的禁止目录。
- 不顺带实现相邻任务或未满足的外部依赖。
- 无真实账号时不读取或生成真实凭证，不调用真实平台副作用。

## 前置依赖

- `GOV-003`
- `GOV-008`

## 输入

- `predecessor_artifacts`
- `tenant_context`
- `idempotency_key`

## 输出

- `GOV-009.implementation`
- `GOV-009.tests`
- `audit_evidence`

## 契约与迁移

契约：

- `packages/contracts/jsonschema/governance.schema.json`
- `packages/contracts/jsonschema/real-account-dependency.schema.json`

迁移计划：

- `packages/db/migrations/versions/20260918_gov_009_account_dependency.py`
## 目录边界

拥有目录：

- `docs/governance`

允许目录：

- `docs/governance`
- `docs/adr`
- `scripts`
- `infra/policies`
- `packages/contracts`
- `packages/db/migrations`
- `tests/unit`
- `tests/integration`
- `tests/contract`

禁止目录：

- `adapters/platforms`
- `deploy/environments/prod`
- `deploy/environments/prod/secrets`
- `secrets`
- `**/*.pem`
- `**/*secret*.json`
- `**/*token*.json`

## 权限、幂等、失败和审计

- 由 `TenantContext` 注入 `org_id` 和 actor；跨租户访问必须拒绝。
- 写命令使用 `Idempotency-Key` 和 payload hash；状态修改使用 `If-Match` 或 expected version。
- 确定性校验失败不重试；临时失败按任务卡与策略退避；未知外部结果转人工核查。
- 记录 `trace_id`、actor、输入/输出版本、Policy 快照、拒绝原因、耗时和成本。

## Given–When–Then

Given：

- `所有前置任务已完成`
- `TenantContext、trace_id 和 Idempotency-Key 可用`

When：

- `执行 GOV-009 的公开用例或内部命令`

Then：

- `明确真实账号获取这个外部依赖的 Owner、最晚到位时间和无账号替代方案。`
- `输出契约、审计事件和指定测试结果可复现`

补充场景：

- Given 真实平台账号尚不可用，When 查看依赖状态，Then `platform_operator` 负责取证、`governance_owner` 对门禁负责，最晚 2026-12-31 17:00（北京时间）取得完整证据。
- Given 未到位或证据不完整，When 到达预警期或截止日期，Then 提前 30 天升级或逾期升级，Distribution Pilot 保持 No-Go。
- Given 无账号阶段，When 运行首期内容流程，Then 只允许 `manual_export` 与 `simulation`，不得宣称真实 OAuth、发布或平台指标。
- Given 未来取得真实账号，When 申请真实分发，Then 必须先验证授权、Scope、地域、Vault、回查等全部证据并重新 Go/No-Go；创建新的 TargetVersion 和 Intent，不得重用 synthetic Intent。

## 实现规格

1. `real-account-dependency-v1.yaml` 记录角色、固定截止时间、每 7 天复核、30 天预警、八类证据和无账号替代方案。当前状态明确为 `unavailable`，完成本治理任务不代表外部账号已到位。
2. 本地判定仅计算里程碑状态并返回无账号允许的交付模式；任何状态变化需新版本与 Go/No-Go，不能仅因日期到达而打开真实平台副作用。
3. 证据库只保留引用，禁止在治理文件或版本表存放真实 Token、Cookie 或密码。
4. Alembic 建立版本化依赖决策表，同一 dependency key/version 不可重复插入。

## 回滚

回退到 `architecture_mvp` 的 `manual_export`/`simulation`，保留逾期升级与审计记录；Alembic 可降级到 `20260918_gov_008` 移除空依赖版本表。回滚不得开启真实平台动作。

## 验证

```text
python -m pytest tests/contract tests/integration --maxfail=1
```

Gate：`gov_009_acceptance`

## 外部依赖

- 无

## 开工前细化

本卡是 bootstrap 任务卡。任务从 `planned` 进入 `in_progress` 前，Owner 必须把通用输入、输出、契约文件和 Given–When–Then 细化为本任务的精确行为，并由一致性检查确认引用存在。

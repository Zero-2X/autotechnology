# GOV-008 — 定义成功指标、成本预算、SLO、RPO/RTO 和人工审批 SLA。

状态：`done`  
阶段：0（范围、决策和治理冻结）  
Owner：`team/governance`  
优先级：`critical` / `P0`

## 目标

定义成功指标、成本预算、SLO、RPO/RTO 和人工审批 SLA。

## 明确不做

- 不修改任务注册表中的禁止目录。
- 不顺带实现相邻任务或未满足的外部依赖。
- 无真实账号时不读取或生成真实凭证，不调用真实平台副作用。

## 前置依赖

- `GOV-002`
- `GOV-003`
- `GOV-007`

## 输入

- `predecessor_artifacts`
- `tenant_context`
- `idempotency_key`

## 输出

- `GOV-008.implementation`
- `GOV-008.tests`
- `audit_evidence`

## 契约与迁移

契约：

- `packages/contracts/jsonschema/governance.schema.json`
- `packages/contracts/jsonschema/operational-targets.schema.json`

迁移计划：

- `packages/db/migrations/versions/20260918_gov_008_operational_targets.py`
## 目录边界

拥有目录：

- `docs/governance`

允许目录：

- `docs/governance`
- `docs/adr`
- `scripts`
- `infra/policies`
- `packages/contracts`
- `packages/db/migrations`
- `tests/unit`
- `tests/integration`
- `tests/contract`

禁止目录：

- `adapters/platforms`
- `deploy/environments/prod`
- `deploy/environments/prod/secrets`
- `secrets`
- `**/*.pem`
- `**/*secret*.json`
- `**/*token*.json`

## 权限、幂等、失败和审计

- 由 `TenantContext` 注入 `org_id` 和 actor；跨租户访问必须拒绝。
- 写命令使用 `Idempotency-Key` 和 payload hash；状态修改使用 `If-Match` 或 expected version。
- 确定性校验失败不重试；临时失败按任务卡与策略退避；未知外部结果转人工核查。
- 记录 `trace_id`、actor、输入/输出版本、Policy 快照、拒绝原因、耗时和成本。

## Given–When–Then

Given：

- `所有前置任务已完成`
- `TenantContext、trace_id 和 Idempotency-Key 可用`

When：

- `执行 GOV-008 的公开用例或内部命令`

Then：

- `定义成功指标、成本预算、SLO、RPO/RTO 和人工审批 SLA。`
- `输出契约、审计事件和指定测试结果可复现`

补充场景：

- Given M1-Core 的合成运行窗口，When 统计可计量工作流，Then 至少 100 个样本且 30 天窗口；缺样本只能报 inconclusive，不能宣称生产 SLO 达标。
- Given 计划成本和已用额度，When 新增工作，Then 按单工作流、日、月上限预判；未知、负数、非有限数或超限均拒绝新成本动作。
- Given R0～R4 HumanTask，When 创建任务，Then 首响截止时间与 `risk-policy-v1.yaml` 的时限和审批人数一致，逾期升级且副作用继续阻断。
- Given 恢复演练，When 计算恢复时间和可恢复事实时间，Then RTO 不超过 240 分钟且 RPO 不超过 60 分钟；缺证据不能通过 Go/No-Go。

## 实现规格

1. `operational-targets-v1.yaml` 与 JSON Schema 冻结成功率、零 Policy 绕过/未授权外部动作、成本预算、运行 SLO、RPO/RTO 和人工首响 SLA。
2. 首期目标：完成率至少 95%，可追溯输出 100%，月可用性 99%，审计记录交付 99.9%，队列启动 P95 不超过 300 秒；这些是项目目标，只有真实窗口及样本证据才可证明达标。
3. USD 成本上限为单工作流 2.00、日 25.00、月 300.00；无真实 Provider 批准前仅做模拟记账，预算判定使用 Decimal 并在未知成本时拒绝。
4. R0～R4 首响时限分别为 1440/480/120/30/15 分钟，审批人数 1/1/1/2/2，与既有风险策略一致；首响不等同于最终批准。
5. `infra/policies/operational_targets.py` 仅提供本地判定，不宣称已运行生产 SLO；新增 Alembic 版本表作为不可重用 key/version 的策略记录边界。

## 回滚

停止引用 `operational-targets/v1`，恢复上一版治理目标；Alembic 降级到 `20260918_gov_007` 可移除空版本表。已生成的运行、审批与成本审计证据必须保留。

## 验证

```text
python -m pytest tests/contract tests/integration --maxfail=1
```

Gate：`gov_008_acceptance`

## 外部依赖

- 无

## 开工前细化

本卡是 bootstrap 任务卡。任务从 `planned` 进入 `in_progress` 前，Owner 必须把通用输入、输出、契约文件和 Given–When–Then 细化为本任务的精确行为，并由一致性检查确认引用存在。

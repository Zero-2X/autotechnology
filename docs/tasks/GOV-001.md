# GOV-001 — 锁定首个垂直领域 `AI 技术与应用工程`、`zh-CN/en-US`、文本内容、第一方知识站和手工导出；未完成范围锁定时不得进入阶段 2。

状态：`done`  
阶段：0（范围、决策和治理冻结）  
Owner：`team/governance`  
优先级：`critical` / `P0`

## 目标

锁定首个垂直领域 `AI 技术与应用工程`、`zh-CN/en-US`、文本内容、第一方知识站和手工导出；未完成范围锁定时不得进入阶段 2。

## 明确不做

- 不修改任务注册表中的禁止目录。
- 不顺带实现相邻任务或未满足的外部依赖。
- 无真实账号时不读取或生成真实凭证，不调用真实平台副作用。

## 前置依赖

- 无

## 输入

- `predecessor_artifacts`
- `tenant_context`
- `idempotency_key`

## 输出

- `GOV-001.implementation`
- `GOV-001.tests`
- `audit_evidence`

## 契约与迁移

契约：

- `packages/contracts/jsonschema/governance.schema.json`
- `packages/contracts/jsonschema/vertical-scope.schema.json`

迁移：

- `packages/db/migrations/versions/20260915_gov_001_governance_scope.sql`
## 目录边界

拥有目录：

- `docs/governance`

允许目录：

- `docs/governance`
- `docs/adr`
- `infra/policies`
- `packages/contracts`
- `packages/db/migrations`
- `scripts`
- `tests/unit`
- `tests/integration`
- `tests/contract`

禁止目录：

- `adapters/platforms`
- `deploy/environments/prod`
- `deploy/environments/prod/secrets`
- `secrets`
- `**/*.pem`
- `**/*secret*.json`
- `**/*token*.json`

## 权限、幂等、失败和审计

- 由 `TenantContext` 注入 `org_id` 和 actor；跨租户访问必须拒绝。
- 写命令使用 `Idempotency-Key` 和 payload hash；状态修改使用 `If-Match` 或 expected version。
- 确定性校验失败不重试；临时失败按任务卡与策略退避；未知外部结果转人工核查。
- 记录 `trace_id`、actor、输入/输出版本、Policy 快照、拒绝原因、耗时和成本。

## 实现规格

1. 创建并锁定 `docs/governance/vertical-scope.yaml`，范围为 `AI 技术与应用工程`，首期重点覆盖 LLM、RAG、Agent、LangChain/LangGraph、Prompt、模型评测、AI 应用架构、部署、可观测性、安全、隐私、版权和合规工程。
2. 将用户提出的八项总体要求写入 `product_capabilities`，并把首期可交付范围与 M1-Plus/M2/M3 后续范围分开。
3. 固定 `zh-CN`、`en-US`、`text`、`first_party`、`manual_export` 和 `simulation`；明确账号、OAuth、Token、真实平台、自动客服发送和生产级媒体渲染暂不在 M1-Core。
4. 使用 `vertical-scope.schema.json` 校验范围字段；该 Schema 加入 `governance.schema.json` union，并登记 `GOV-001`。
5. 范围锁定后不得原地编辑；后续扩展必须增加 `scope_version` 并重新执行治理、Policy、Rights、QA 和 Approval。

## Given–When–Then

Given：

- `所有前置任务已完成`
- `TenantContext、trace_id 和 Idempotency-Key 可用`

When：

- `执行 GOV-001 的公开用例或内部命令`

Then：

- `锁定首个垂直领域 `AI 技术与应用工程`、`zh-CN/en-US`、文本内容、第一方知识站和手工导出；未完成范围锁定时不得进入阶段 2。`
- `输出契约、审计事件和指定测试结果可复现`

## 验证

```text
python -m pytest tests/contract/test_gov_001_scope.py tests/contract/test_gov_001_migration.py --maxfail=1 -q
```

Gate：`gov_001_acceptance`

## 外部依赖

- 无

## 开工前细化

本卡是 bootstrap 任务卡。任务从 `planned` 进入 `in_progress` 前，Owner 必须把通用输入、输出、契约文件和 Given–When–Then 细化为本任务的精确行为，并由一致性检查确认引用存在。


## 实施证据

- 已锁定范围文件：`docs/governance/vertical-scope.yaml`。
- 已完成总体要求到首期范围的映射：`docs/governance/GOV-001-DECISION.md`。
- 已完成契约测试：`tests/contract/test_gov_001_scope.py`。
- 本任务写入治理文档、契约、测试和最小的版本化 `governance_scopes` 表结构；不生成账号或凭证，不调用平台副作用。
- 验收运行：`python -m pytest tests/contract/test_gov_001_scope.py tests/contract/test_gov_001_migration.py --maxfail=1 -q`，结果 `3 passed`。
- 完整验收证据：`docs/governance/GOV-001-EVIDENCE.yaml`，`trace_id=631d7201-c07e-46fe-afe2-f7609e116272`。

## 补充场景

- 重复执行相同范围版本时返回同一 scope 版本，不创建第二个 `scope_key/scope_version`。
- 尝试在 `scope_version: 1` 上原地修改语言、内容类型或分发模式时必须拒绝，并要求新版本。
- 迁移脚本在空数据库和已存在表上均可重复执行，不删除已有范围事实。

## 回滚

- 代码回滚：保留 `scope_version: 1` 和已写入的审计证据，回退读取方到上一兼容版本；不删除锁定范围。
- 数据库回滚：不执行破坏性回滚；若迁移验证失败，暂停后续任务并保留 `governance_scopes` 表，使用新修订版本修复。
- 范围变更：不得覆盖 V1；创建新的 `scope_version` 并重新通过治理、Policy、Rights、QA 和 Approval。

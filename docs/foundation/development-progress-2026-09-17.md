# 开发进度复核 — 2026-09-17

机器登记：V2.4 共 157 项，24 项 done、133 项 planned；没有 in_progress 或 blocked。完成项是 GOV-001～006，以及 FOUND-000、001、002、003A、003B、003D、004A～004E、005、006A、006B、007A～007D。完成数量不代表工作量百分比。

当前已有基础运行入口、synthetic 存储和任务队列、Outbox、租约、失败重试/重放、日志指标追踪、OpenAPI、事件和对象 Schema。业务领域链路、真实 PostgreSQL 并发、真实 S3、正式 Worker 闭环和平台发布尚未完成。V3 编排任务需要独立验收，不能由 V2.4 的完成状态推定完成。

README 截至 FOUND-007B 的描述和早期审计的测试数已落后；最新完成任务证据是 FOUND-007D。当前目录没有 Git 元数据，无法提供分支、提交或未提交差异；本次使用机器注册表、文件、任务证据和可重复测试复核。起始全量测试实际得到 274 passed。

## 本轮开发范围

按总体审计“进入 FOUND-008 前先处理未关闭问题”的约束，本轮绑定 FOUND-007B，修复递归 payload property 兼容检查漏检，并校验排序键和消费者去重键。保留原事件文件和冻结基线，不重生成基线、不更改迁移或任务完成数量。

递归检查覆盖嵌套字段、必填项、数组元素与长度、additionalProperties、uniqueItems 和类型集合扩展。对引用、组合和条件等无法证明兼容的关键字变化保守拒绝。新增反例包含数组内对象字段的 minimum 收窄，正例包含 nullable 扩展和 integer → number。

后续续建已补充根级 payload 全约束比较和外围 Schema 布局门禁，拒绝没有冻结依赖的 v1 payload 引用，消除只比较引用字符串的绕过路径。新增 EventSchemaRegistry，按事件类型和版本选择深复制、不可覆盖的离线校验器；162 个现有事件、合成 v1/v2、递归引用、嵌套 ID、缺失引用、数值与错误脱敏均有运行测试。此处多版本完成的是 Schema 选择与校验，业务消费者派发和持久化去重仍需运行时装配。

完整通用 JSON Schema 包含关系不是现有 v1 检查器可证明的能力；未支持的组合语义由明确拒绝/版本评审保护。仍需处理总体审计中的运行时、真实数据库验证和存储键策略。FOUND-008 仍是下一项计划功能，任务卡需先细化；不能将本轮修复当作全部历史问题关闭。

## 验证与回滚

验证命令和本轮结果记录于 `docs/foundation/FOUND-007B-EVIDENCE.yaml` 的 continuation_2026_09_17。回滚只撤回本轮 checker 与回归测试增量，保留历史 Schema、基线、数据库和历史证据；本轮没有数据库变更，无需降级迁移。

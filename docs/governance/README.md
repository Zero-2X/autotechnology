# GOV-001 治理范围

本目录保存项目首个纵向切片的治理范围和锁定证据。范围文件是规划事实，不包含账号、Token 或平台凭证。

当前锁定文件：

- `vertical-scope.yaml`：机器可读范围和能力边界
- `GOV-001-DECISION.md`：人类可读决策、假设和验收证据

修改规则：首个范围一旦锁定，不得原地修改。若未来增加垂直领域、语言、内容形态或分发能力，创建新的 `scope_version`，并记录 supersession 与重新验收任务。

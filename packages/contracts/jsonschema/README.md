# JSON Schema source

Each schema file must declare `$schema`, `$id`, `type`, `required`, `additionalProperties: false`, enum constraints, and cross-field `oneOf` rules where applicable. This directory is generated incrementally by the contract tasks; do not replace schemas with Markdown examples.
